name: rettulf_pubdb_collector_helper
description: Dart analyzer helper for rettulf-pubdb collector pipelines.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

dependencies:
  analyzer: ^13.0.0
  pub_semver: ^2.2.0
